groups:
  - name: example-alert
    rules:
      - alert: HighCPUUsage
        expr: process_cpu_seconds_total > 80
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "High CPU usage detected"
